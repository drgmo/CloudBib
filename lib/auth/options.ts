import type { NextAuthConfig } from "next-auth";

/**
 * Auth.js configuration — will be filled in Step 3 (Auth und Rechte).
 * For now this exports a minimal placeholder so the project compiles.
 */
export const authOptions: NextAuthConfig = {
  providers: [],
  // Will add PrismaAdapter, session strategy, callbacks in Step 3
};
